#!/bin/bash
# Deprecated: Ralph config is now ralph-config.md (one file at project root).
# Copy template/ralph-config.md.template to your project as ralph-config.md and edit.
# See docs/RALPH_CONFIG.md.
